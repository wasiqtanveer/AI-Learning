dictionary = {
    "go" : "jaana",
    "stop" : "rukna",
    "fetch" : "jaana k lana",
} 

print(dictionary["go"]) # calling by key and printing pair is not possible through lists
print(dictionary["stop"])
print(dictionary["fetch"])

# it is unordered
# it is mutable
# it is indexed
# can't contain duplicate keys
